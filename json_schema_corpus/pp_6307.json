{
  "$schema": "http://json-schema.org/schema#",
  "endpoint": "/query",
  "description": "testing the sending of query(string)",
  "methods": [
    "get",
    "post",
    "put",
    "delete"
  ],
  "query": {
    "key": "value"
  },
  "type": "object",
  "properties": {
    "key": {
      "type": "string",
      "enum": [
        "value"
      ]
    }
  },
  "required": [
    "key"
  ]
}