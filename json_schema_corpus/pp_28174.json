{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "organizationLookupList",
  "id": "organizationLookupList",
  "type": "object",
  "properties": {
    "available": {
      "type": "number",
      "description": "The number of items available for this collection."
    },
    "returned": {
      "type": "number",
      "description": "The number of items returned for this collection."
    },
    "link": {
      "$ref": "link.json#",
      "description": "A self referencing link to the collection."
    },
    "items": {
      "type": "array",
      "items": {
        "$ref": "organizationLookup.json#"
      },
      "description": "An array for Organization Lookup items."
    }
  }
}