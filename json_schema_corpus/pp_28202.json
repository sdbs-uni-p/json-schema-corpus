{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "orderNotification",
  "id": "orderNotification",
  "description": "A notification initiated by the Seller to the Buyer providing subsequent status information on \u201cAcknowledged\u201d orders such as service delivery confirmation, error, and/or jeopardy messages.",
  "type": "object",
  "required": [
    "objectType",
    "orderId",
    "buyerId",
    "sellerId",
    "buyerPurchaseOrderNumber",
    "buyerOrderVersion",
    "orderStatus"
  ],
  "properties": {
    "objectType": {
      "type": "string",
      "enum": [
        "orderNotification"
      ],
      "description": "The type of the object, orderNotification."
    },
    "orderId": {
      "type": "string",
      "description": "Unique identifier for the order that is generated by the Seller when the order is initially accepted via an API.",
      "maxLength": 45
    },
    "buyerId": {
      "type": "string",
      "description": "The ID of the Service Provider (Buyer) organization placing the order.",
      "maxLength": 25
    },
    "sellerId": {
      "type": "string",
      "description": "The ID of the Partner/Access Provider (Seller) organization receiving the order.",
      "maxLength": 25
    },
    "buyerPurchaseOrderNumber": {
      "type": "string",
      "description": "A number that uniquely identifies a order within the buyer's enterprise.",
      "maxLength": 16
    },
    "buyerOrderVersion": {
      "type": "string",
      "description": "The version number that the Buyer uses to refer to this particular version of the order.",
      "maxLength": 2
    },
    "notificationType": {
      "type": "string",
      "enum": [
        "CANCELLATION",
        "COMPLETION",
        "CONFIGURATION",
        "CONFIRMATION",
        "ERROR_OR_INFORMATION",
        "JEOPARDY",
        "REJECTION",
        "STATUS_UPDATE"
      ],
      "description": "The type of order notification."
    },
    "notificationVersion": {
      "type": "string",
      "description": "An integer string that indicates the version of the Notification. Tha value must be incremented on each Notification sent by the Seller to the Buyer..",
      "maxLength": 2
    },
    "projectId": {
      "type": "string",
      "description": "An identifier that is used to group Orders that represent a unit of functionality that is important to a Buyer. A Project can be used to relate multiple Orders together.",
      "maxLength": 16
    },
    "requestedCompletionDate": {
      "type": "string",
      "format": "date-time",
      "description": "Identifies the customer's requested completion date for the Order. Formatted as YYYY-MM-DD (eg 1997-07-16) or YYYY-MM-DDThh:mmTZD (eg 1997-07-16T19:20+01:00)."
    },
    "expectedCompletionDate": {
      "type": "string",
      "format": "date-time",
      "description": "Identifies the date the Seller expects to complete the Order. Formatted as YYYY-MM-DD (eg 1997-07-16) or YYYY-MM-DDThh:mmTZD (eg 1997-07-16T19:20+01:00)."
    },
    "completionDate": {
      "type": "string",
      "format": "date-time",
      "description": "Identifies the date the Seller completed the Order. Formatted as YYYY-MM-DD (eg 1997-07-16) or YYYY-MM-DDThh:mmTZD (eg 1997-07-16T19:20+01:00)."
    },
    "orderStatus": {
      "type": "string",
      "enum": [
        "ACKNOWLEDGED",
        "IN_PROGRESS",
        "CANCELLED",
        "COMPLETED",
        "REJECTED",
        "PENDING",
        "HELD",
        "FAILED",
        "PARTIAL"
      ],
      "description": "The status of the order."
    },
    "orderCancellationDate": {
      "type": "string",
      "format": "date-time",
      "description": "Identifies the date that the Seller cancelled the Order."
    },
    "orderCancellationReason": {
      "type": "string",
      "description": "An optional free-form text field for the seller to provide additional information regarding the reason that the order was cancelled.",
      "maxlength": 300
    },
    "sellerOrderContact": {
      "$ref": "contact.json#",
      "description": "TIdentifies the name of the person or office that is handling the Order within the Seller's organization."
    },
    "notes": {
      "type": "string",
      "description": "Free form text to clarify or explain the notification.",
      "maxLength": 300
    },
    "billingAccountNumber": {
      "type": "string",
      "description": "Identifies the Billing Account to use to bill the ordered products.",
      "maxLength": 13
    },
    "sellerOvcId": {
      "$ref": "idAssignment.json#",
      "description": "The ID assigned by the Seller to the OVC on this Order."
    },
    "sellerUniIds": {
      "type": "array",
      "items": {
        "$ref": "idAssignment.json#"
      },
      "minItems": 1,
      "uniqueItems": true,
      "description": "An array of The IDs assigned by the Seller to the UNIs on this Order."
    },
    "sellerEnniIds": {
      "type": "array",
      "items": {
        "$ref": "idAssignment.json#"
      },
      "minItems": 1,
      "uniqueItems": true,
      "description": "An array of The IDs assigned by the Seller to the ENNIs on this Order."
    },
    "enniSvlanIds": {
      "type": "array",
      "items": {
        "$ref": "svlanIdAssignment.json#"
      },
      "minItems": 1,
      "uniqueItems": true,
      "description": "An array The S-VLAN IDs (or ID ranges) assigned by the Seller to ENNIs on this Order."
    },
    "uniSvlanIds": {
      "type": "array",
      "items": {
        "$ref": "svlanIdAssignment.json#"
      },
      "minItems": 1,
      "uniqueItems": true,
      "description": "An array of the S-VLAN IDs (or ID ranges) assigned by the Seller to UNIs on this Order."
    },
    "correctionOrderExpected": {
      "type": "boolean",
      "description": "If true, the Buyer is expected to send a corrective (supplemental) order as a replacement order."
    },
    "correctionOrderDueDate": {
      "type": "string",
      "format": "date-time",
      "description": "Date by which the corrected/amended order must be sent to the Seller to ensure that the order is not CANCELLED. Formatted as YYYY-MM-DD (eg 1997-07-16) or YYYY-MM-DDThh:mmTZD (eg 1997-07-16T19:20+01:00)."
    },
    "orderMessages": {
      "type": "array",
      "items": {
        "$ref": "orderMessage.json#"
      },
      "minItems": 1,
      "uniqueItems": true,
      "description": "An array of messages associated with this Order."
    }
  }
}